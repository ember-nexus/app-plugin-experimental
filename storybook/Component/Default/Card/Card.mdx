import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import * as DefaultStories from './Default.stories';

<Meta of={DefaultStories} />

# `ember-nexus-default-card`

An card element used as a fallback or default renderer. Not specialized towards a single component, but usually able
to display at least generic data of elements.

<Canvas of={DefaultStories.Default} />
